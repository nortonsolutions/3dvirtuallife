import {Entity} from './entity.js'




class EntityFactory {

    constructor(entityTypes) {
        this.entityTypes = entityTypes;
    }

    create(entityType) {
        switch (entityType) {



        }
    }
}

export {EntityFactory};